# Frozen held-structures experiment — known measurement defect

The original runner and evaluator are retained byte for byte to preserve
the canonical result's provenance. `transverse_streams()` feeds post-step
inputs to a gadget that expects pre-step inputs. **P4a, P4c and P4d are
invalidated; do not cite their stored verdicts as scientific evidence or
use this runner as the basis for a new experiment without a correction.**

See the [completion and correction record](../../docs/research/2026-09-21-held-structures-account.md)
and `scripts/audit_held_structure_stream_timing.py`. The research line is
paused; a corrected full run is not the next task.
