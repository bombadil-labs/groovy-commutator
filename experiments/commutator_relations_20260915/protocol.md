# Do completion-independent relations survive translation removal and lifting?

Frozen before implementation of this reanalysis. Author: Codex (OpenAI), /root, 2026-09-15. Myk has granted standing Gate 1 approval and explicitly requested this next test. Independent review precedes merge. The prior census and its headline counts have already been seen; this is an algebraic reanalysis with a targeted validation, not a blind classifier evaluation. No new Wolfram labels or thresholds will be fitted.

## Question

The prior audit proves G=c XOR A*u, where each free physical query supplies one shared variable. We ask how many relations among ambiguous G cells remain after removing translated copies of the same pointed physical state, and whether those remaining relations hold at the next lift. The phrase "identity without essence" motivates studying relationships whose values are fixed although their individual endpoints are not. It is not itself an empirical claim.

## Exact shortcut over the complete prior census

Use all 2,048 contracts in results/commutator_completion_20260915.json: 256 roots, widths seven/eight, D2 finite and complete first-lift tables, and finite D3/D4. A source event consists of source word, transverse phase and longitudinal position. Simultaneously translating the source and the pointed longitudinal position is a free action of the width-w cyclic group, preserving the physical query and the affine constant. Choosing position zero retains one representative of every orbit. Periodic source words do not invalidate this: the pointed position makes the action free.

If M is the number of ambiguous cells and U the number of queried free keys, the quotient has M/w ambiguous events, U variables, and exactly M/w-U independent relations among those events. From prior equal/opposite ambiguous-pair counts E/O, the quotient pair counts are

E' = (2E - M(w-1))/(2w^2), O' = O/w^2.

Require exact integral nonnegative results. This removes all longitudinal translation copies; it may leave other whole-state translation equivalences. Zero remaining relations is already decisive and cannot be repaired by further quotienting. Previously displayed width-seven counts for 54/110 at D3 suggest this zero case. We will check the entire census rather than infer class specificity from those examples.

## Targeted physical validation and full spatial translation quotient

Fixed panel: roots 0,4,18,30,54,90,110,124,126,137,147,193,204; both widths; D2,D3,D4 (78 records). This includes every symmetry variant of the two core families and simple/chaotic controls. Reuse the original six-field source archive and the full-input D2 table; verify their hashes. Do not regenerate the lift or simulate new root trajectories. Reconstruct native successors from the archived family as in the preceding audit, with the elementary root truth table as a consistency check.

Recover the same symbolic fields and verify their prior counts. Keep longitudinal position zero, then identify events whose entire pointed source configurations Y are identical after centering their transverse coordinates. Full configurations, rather than finite local patches or difference fields alone, define this second quotient. This removes exact spatial-translation redundancy without erasing genuine ambiguity from different source states that happen to share a difference neighborhood. Use exact packed bytes; hash collisions must not define equivalence. Verify that each whole-state equivalence class has the same G variable/constant. Record full spatial orbit counts, ambiguous orbit counts, remaining variables, relation rank and signed pair counts.

## Address correspondence across lifts

For D2-to-D3 and D3-to-D4 in the fixed panel, send a parent event to its six child events at the same source word and old coordinates, one for each new-axis phase. This is a declared physical address map, not an assumed native-G transport theorem. Verify that each fixed-phase map respects the full-state quotient and is injective on its classes.

Test seven fixed readouts: the six individual child G bits and their decoder-style XOR at phases four and five. Readouts are exact affine expressions in child completion variables; repeated variables cancel. For each pair of distinct parent orbit classes sharing a free parent variable, ask whether the child readout pair has the same variable-support signature, so their parity is completion-independent. Separate pairs with both child expressions fixed from pairs that share a nonempty variable support. Count whether the fixed child parity agrees with or reverses the parent parity. Report a missing denominator as not applicable, not zero survival. Do not select a readout or threshold by performance.

If a floor has no remaining free relations after the longitudinal quotient, check whether each six-phase child block is wholly ambiguous or wholly fixed. With distinct independent variables at every ambiguous event and disjoint all-free blocks, any fixed nonconstant Boolean readout of those six variables can vary independently across blocks. This conditional algebraic observation may rule out a broader class of sibling-only readouts, but only where its conditions are actually verified. It does not concern overlapping spatial readouts, added constraints on completions, or all-dimensional transport.

## Preservation, review, budget and limits

Preserve all census reanalysis rows, all targeted per-event symbolic data after position-zero selection, full-state orbit IDs, and all seven readout-transfer summaries. Retain examples of surviving/reversed relations if present. The exact prior archive and complete first-lift table are immutable inputs. Independent review must rederive the counting formulas and reconstruct the fixed panel using a different physical-key implementation; it need not repeat the prior full 1,536-record census.

Primary scientific budget: 10 minutes, 2 GiB process RSS, excluding recovery of previously preserved inputs. Atomically checkpoint the small selected records and label a budget stop as censored. Perform synthetic tests before scientific evaluation. Run science and the independent physical replay outside Actions; automatic CI compiles and checks fixed source hashes.

Each width is a separate report. A single full-input D2 completion still shares equal physical keys across widths; no joint completion entropy is obtained by summing those reports. D3/D4 freedom remains relative to each finite source-family table. No recurrence, Class IV classification, intrinsic dimension, adaptation or philosophical conclusion follows just from a nonzero relation count. A negative result concerns these observables and declared correspondences, not the existence of a beam or every possible discriminator.
