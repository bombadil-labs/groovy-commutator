# Recovered protocol and prospective review

Recovered by Codex (OpenAI), /root, 2026-09-15, from the intact prior workspace.
The original protocol and protocol-freeze.json are preserved byte for byte.
No experiment implementation or output was present. The current user asked:
“Can you recover the details from that conversation and try it here?”

Independent pre-implementation review: Codex (OpenAI), /root/relations_review,
2026-09-15. Reviewed protocol SHA-256:
`7ae1ff9d736e5c6efdd5ded582f2d9ec1297fc76757cf02f01b5670d11feed0b`.
Reviewer approved the fixed census, 78-record physical panel and seven readouts.
This review is prospective for this resumed experiment; it does not backdate
review of the prior completed census or the original protocol freeze.

Two clarifications accepted before implementation: the sibling-only no-go
requires zero relation rank on the CHILD floor, plus disjoint all-free child
blocks. A failed quotient-map or injectivity check is reported as invalid
address transport with transfer results not applicable; failing classes are
never selectively dropped. No domain, readout or threshold changes.

The prior commutator source's local copy had one extra trailing newline.
The exact source fetched from main matches the prior result's SHA-256 after
preserving its exact final newline. Raw archive/table hashes are checked before
evaluation. All science runs locally, outside Actions.
