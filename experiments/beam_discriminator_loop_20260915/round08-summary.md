# Round 8 outcome

The unchanged predictor passes all declared density stress cases:54 and110 each8/8; all84 undisputed negatives0/8; disputed1063/8 and410/8. Runtime179.069 seconds. Together R6/R8 give each core12/12 and0/1008 undisputed negative run-level positives. The denominators describe dependent tests of88 representative families, not1008 independently sampled kinds of automata. Only two positive symmetry families exist in the core label set.

The input-density result is stronger than the original mean-at-density1/2 discovery, but it does not quantify all-state behavior or rule universality. Exact periodic inputs remain special. The q cutoff is still a binary-representation statistic. Do not retune106's near-boundary behavior into a categorical claim.

Next, think fresh: use the remaining rounds for the two unresolved representation questions. Measure interval-tail estimates under actual lifted macrocolumns, retaining zero-count censoring, and verify the finite predictor under matched reflection/complement transformations. A clean classification table alone is not the beam-level explanation the user asked for.
