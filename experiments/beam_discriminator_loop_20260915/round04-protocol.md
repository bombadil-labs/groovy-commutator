# Round 4: recurrence defects as a structural lead

Protocol review: none at freeze; run authorized by Myk 2026-09-15 (standing ten-round authorization). Evaluation precedes independent review.

Question: can localized departures from simple spacetime recurrence distinguish the two core Class IV families while rejecting fixed, periodic and chaotic controls? A beam-local conjugacy can transport recurring domains and localized departures, whereas raw table overlap changed almost completely by D3. This is an exploratory lead, not a new definition of Class IV.

Use all 88 root symmetry representatives, unchanged archived labels, one fixed random input seed 6041501, Bernoulli(1/2), width 512, burn 512, retain 520 frames. For each p=1..8 and v=-p..p, compute delta(x,t)=X(x,t) XOR X(x+v,t+p) for t=0..511. Select the smallest mean mismatch without labels; tie order is p, absolute v, then v. For the selected recurrence record mismatch density, unnormalized and normalized spatial covariance at lags 1,4,16, mean and maximum consecutive zero runs, and raw temporal activity. Save every candidate mismatch rate and the packed trajectory.

Diagnostic: exact periodic rules should have zero residual; independent noisy residuals should lack persistent spatial organization. Class IV may occupy a distinct intermediate structure, but Class II rules 9/73 and stochastic-domain Class III rules 18/126 are explicit adversaries. Do not choose a class threshold in this round. Runtime budget two minutes. If scalar organization fails, inspect what it misses before proposing another score.

Background: Rupe and Crutchfield (https://arxiv.org/abs/1801.00515) use spacetime domains and localized departures to identify coherent structures. We are testing a simple recurrence residual, not implementing their local causal-state method or claiming priority.
