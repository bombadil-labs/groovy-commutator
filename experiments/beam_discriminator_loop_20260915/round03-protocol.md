# Round 3: persistent native obstructions at the fourth floor

Freeze before evaluation, standing user approval. Follow exactly the62D3w7and56D3w8incompatiblepairs identified in round2 into the archivedD4of the same root/width. Build only the84/72requiredroot tables. This is a persistence test on the selected residual pairs, not a complete D4 graph or a search for newly introduced incompatibilities.

Extract exact physical bits at archivedrepresentatives using the same provenperiod-sixcompression asround2, now6x6x6x5bits. Compare forced masks on sharedphysicalkeys. Recordshared0,shared1,conflicts and exact first witnesses, per-pair changes, roots and core54/110participation. Check per-table unique-key counts against the archive. Preserve all table files/hashes and memberprovenance.

Diagnosticbet: a class-relevant persistentobstruction should remain selective throughD4andbothwidths. If no persistentconflicts remain, retire this simple fixed-radius persistence discriminator rather than continue depth without a new mechanism. This does not prove a beam has no intrinsic structure, and does not assume arbitrary-parent generality. Ten-minute/1.5GiBbudget, outsideActions.
