# Round 7: actual support transport through six dimensions

Protocol review: none at freeze; run authorized by Myk2026-09-15. Evaluation precedes independent review.

Question: does the proposed support theorem hold in materialized ancestral six-field representations through D6, and which measured quantities change? This tests the encoding and its explicit macrocolumn decoder. It does NOT independently certify a phase-free native6D local rule, nor arbitrary higher-dimensional parents.

Fix roots0,18,54,73,110,126,204; one new seed6041521, width521, density1/2, burn512. Compare (a) baseline and a single central-bit perturbation, and (b) baseline and its R4-selected time/space recurrence partner. For t=0,1,8,32,64,128, form the root trajectory lookahead required to build J_d for every d=1..6. Use the same six fields; comparisons at parent dimension>=2 include one step in its newest existing lifted axis.

At each floor, verify decoder F4 XOR F5 exactly recovers the prior representation and recursively the root state. Let A be source disagreement positions and B_d the positions with ANY differing macrocolumn bit. Check A subset B_d subset A thickened by R=2(d-1). Record counts, support diameter for perturbations, extrema shifts and physical-cell mismatch density. No-wrap support statements for central perturbations use radius128+10<260. Recurrence support uses the periodic ring for the finite check.

Theorem under review: J_d has root-line locality radius<=2(d-1) and a same-column decoder, so these inclusions hold for every source pair. Boundedness and asymptotic spreading velocities survive every fixed floor. For a spatially stationary pair ensemble, P_A(empty interval length L+2R)<=P_B(empty interval length L)<=P_A(empty interval length L). If the asymptotic exponential tail rate exists, it is invariant. Finite density, covariance and tail estimates need not be. Budget3minutes/1GiB memory.
