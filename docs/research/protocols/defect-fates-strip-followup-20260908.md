# Post-census follow-up: a finite strip encoding

This hypothesis was formulated AFTER the frozen 32-tick census. In the zero
logical background, masks 6 and 24 remain two rows high and have the mass
sequence of a Rule-90 pulse. No whole-shape recurrence certificate was found,
so the predeclared perturbation-of-certified-objects stage has no eligible case.
The following is a separate algebraic follow-up, not a predeclared discovery.

Let B(y,2i)=0 and B(y,2i+1)=1 at every row. Define U(s) by replacing just rows
zero and one with (0,1 XOR s_i) and (s_i,1), respectively, in physical block i.
All other rows stay B. Test the conjecture F^2 U = U phi90 for ALL logical
inputs, with no translation at the two-tick sample. Mask6 is a single logical
flip in U; mask24 is its vertical translate by one row.

The exact local check enumerates the eight logical triples (-1,0,+1), physical
input columns -2..3, rows -4..5. Two shrinking updates leave columns 0..1,
rows -2..3. Require the entire output to equal B outside rows0/1 and U(l XOR r)
inside. These rows include every possible vertical change; horizontal locality
then proves the claim on the infinite line and all logical ring widths.
Also compare the first-tick formula:
row0=(1 XOR s_(i-1) XOR s_i,0), row1=(1,s_i XOR s_(i+1)),
all other rows B complemented. Verify with primary arrays and independent
Boolean truth-set evolution. If any equality fails, retain the counterexample.

If the identity passes, the finite action toggling exactly (0,2i+1) and
(1,2i) satisfies Atilde_i U=U A_i. This proves preservation of arbitrary finite
matched action words by induction. Corroborate on every logical input at ring
widths5/7, four coarse ticks, and every three-action word over no-op/flipcell0
at width5. Use sufficient vertical padding for the whole horizon; no vertical
torus. Independently compare every fine field via packed Boolean updates.

Do not describe this as physical recovery from the old code, a newly born
structure after a transient, or a Class-IV result. On the specified background
the exceptional initial masks are already in U's invariant family. Counts,
scope, discovery chronology, and the fixed background requirement must remain
explicit in the public account.
