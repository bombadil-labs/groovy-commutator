# Frozen next experiment: retain both spatial differences

Date: 2026-09-10. Status: planned, not executed. This is a separately frozen change of observation after the transverse-only census. It does not alter or rescue that completed result retroactively.

## Fixed law, changed observation, explicit cost

Retain the same66 source words and unchanged ordered axial rule G_(r,2). Replace the single observation T_y with the pair J(X)=(T_x(X),T_y(X)), where T_x compares(x,y) with(x+1,y), and T_y compares(x,y) with(x,y+1). Keep both components; no outcome-dependent orientation choice is allowed.

On the connected infinite square lattice, equal full J fields imply that the source fields agree everywhere or are global complements. The observation therefore erases one global binary choice, in place of the transverse-only observation's independent baseline bit per column. This is two stored component bits per lattice site, versus one for T_y and one for the original source; it is not a raw storage compression claim. Realizable gradient fields satisfy compatibility constraints around loops, so arbitrary two-component fields are not automatically valid gradients. A preferred ambient completion outside that image is not supplied.

## Exhaustive local budgets

For all66 sources, test a deterministic two-output local cap at Moore radius R=0 and R=1. For R=1 the source support is the4-by4 square of coordinates x,y=-1,0,1,2 with the corner(2,2) omitted:15 source sites,32,768 assignments. It covers the three3-by3 G windows needed for J(G(X))(0,0), and every edge needed for the18 current gradient bits at x,y=-1,0,1. R=0 retains only the two gradient components at(0,0).

Enumerate every source assignment. Either construct the cap on all reachable observed neighborhoods or retain the first conflicting source pair for each rule/budget. Mark unrealizable neighborhoods explicitly or represent only the reachable table with a declared deterministic encoding. Independently audit the source outputs using direct Boolean shrinking arrays, separate from the integer truth-table pipeline. Retain constants, projections, and selected affine controls without presuming they exhaust the passing rules.

## Complement criterion and finite controls

Before drawing an all-radius conclusion, derive the relevant condition from the full observation fibers. For each source evaluate the ordered G truth table on all512 three-by3 patches and their complements. Distinguish a constant output difference from self-duality; constant laws must remain valid controls. Establish necessity and sufficiency on the infinite lattice with an explicit locality argument, rather than treating finite non-conflict as a proof.

For each source also enumerate all512 states of the periodic3-by3 lattice. Compare full observed pairs and their next values, verify every passing local cap, and independently compare native lattice passes with direct local evaluation. Preserve any aliasing that hides an infinite-lattice obstruction. Any upgrade of a local conflict to a complete-field counterexample must state and verify its extension, including the omitted corner.

## Decision

Commit implementation before evaluation and save canonical outputs with CI reproduction. Compare the accepted source families against the transverse-only result. If adding one more derivative component admits additional nonlinear laws, state the extra information, storage cost, image constraints, and exact proof scope. If it fails, retain the obstruction without generalizing it to every observation. Treat this as a comparison of different retained information under one fixed law, not an invertible renaming of the earlier observation, a canonical interpretation, or a self-assembly test.
