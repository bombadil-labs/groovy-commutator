# Frozen next experiment: do transverse differences evolve autonomously?

Date: 2026-09-10. Status: planned, not executed. This protocol follows the completed guard-free axial census. Its construction and budgets are chosen before its outcomes.

## Fixed family and observation

Use the unchanged ordered binary rule G_(r,2)=F_(r,2) composed with F_(r,1), for every r in the exact replication-compatible set:0,255 and every even word from128 through254. Retain ordered axes even for rules whose axial operators commute. Do not filter by class, orbit appearance, or the existence of an anticipated cap.

Observe the transverse difference T(X)(x,y)=X(x,y) XOR X(x,y+1). Its zero set is precisely the literal-replication beam. Ask whether T(G(X)) is a function of T(X) alone, at declared Moore radius R=0 and R=1. Keep the original binary ambient state and update cadence; no added correction row, source retention, or altered embedding is part of this attempt.

## Exhaustive local test

For each of66 sources enumerate every3-by4 binary source patch, with x=-1,0,1 and y=-1,0,1,2:4,096 patches. Compute the next transverse difference at(0,0) from the two overlapping3-by3 G windows. Compute the nine current T values at x,y=-1,0,1. For R=0 use only T(0,0); for R=1 use all nine. These source windows cover every possible local input with no boundary assumptions.

For each rule/budget, either construct a complete truth table on reachable T neighborhoods (mark any unreachable entries explicitly) or retain the first pair of source patches with the same observed neighborhood and different next output. Increasing radius or including the source field would be a separate rescue, frozen before evaluation. A failed finite budget alone is not failure of every autonomous description.

Audit all local source outputs with an independent direct Boolean shrinking-array evaluator. Recover constant and projection controls and the affine difference identity T(G(X))=L(T(X)), with L the linear part of G. Do not presume these controls exhaust the passing rules. Record complete counts, exact lists, witnesses, and deterministic checksums.

## Full-observation finite controls

For each source, enumerate all512 states on the periodic3-by3 lattice. Compare the full current T fields and their next T fields. If a fiber has two different next observations, retain the first pair. Periodically extending that pair gives identical full infinite T fields with different next T, hence a proof that no deterministic update on T alone exists for that source, at any radius. A torus without a collision proves only the finite statement unless accompanied by another proof.

Compare native lattice evolution and direct local evaluation independently. For every passing local cap, verify its predicted next T on all finite states. These controls are not a test of self-assembly, defect healing, or intrinsic dimensional rank.

## Decision and publication

Commit implementation before execution. Save canonical JSON and CI reproduction. Compare autonomous transverse laws, finite-radius obstructions, and any full-field impossibility witnesses separately. State what remains invariant (T=0) versus what has a closed dynamics off that set. If correction or base information is needed, specify that as the next question rather than adding it silently. Preserve all failures and distinguish this observation-relative result from a universal obstruction to higher-dimensional organization.
