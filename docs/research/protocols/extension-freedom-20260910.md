# Frozen experiment: extension freedom around the Rule32 encoded image

Date: 2026-09-10. Status at freeze: planned, not executed. This is a control for the user's clarified dimensional-beam objective, not a new claim of naturally generated dimensions.

## Question and prior information

How much of an ambient update is fixed by requiring exact Rule32 derivative/correction evolution? Separate the encoded data image from its extension, program syntax from induced functions, and neutral program transport from changed represented data dynamics.

Prior results already give K_1(S)=(U,V)=(A_0(S),A_1(S)), U'=F_32(U) xor V and V'=F_128(V), 15 realized six-bit tuple neighborhoods, and seven realized top neighborhoods excluding101. Therefore128 and160 are predicted compatible top ECA words; this is a nominated control derived from earlier evidence, not a newly discovered candidate.

## Frozen logical census

1. Enumerate all128 source words on [-3,3], without periodic boundary. Form the radius-one patch (U_left,U_center,U_right,V_left,V_center,V_right) and target pair K_1(F_32(S)) at the center. Reconstruct these using both truth-table composition and independent shrinking-window Boolean evolution. Report the full forced partial two-bit map and all unrealized input patches.
2. For unrestricted binary pair updates of radius one, count free truth-table bits: two outputs for each unrealized six-bit patch. State the exact count of ambient maps as a power of two. This grammar is broader than the existing table-chain interpreter; do not claim all maps counted are executable by it.
3. Enumerate all256 top ECA words and retain those agreeing with V's required next value on all source windows. Save a first source-window counterexample for every rejection.
4. Enumerate all65,536 native top tuples(q,h). Fix the top positive guard to0 and read U_center from below. The effective input is (V_left,V_center,V_right,U_center); evaluate h[4*q[V_triple]+U_center]. Hold bottom program(32,60) fixed. Report passing stored tuples, distinct effective four-input functions and syntax multiplicities. Retain a deterministic first witness for each rejected tuple using a compact grouped table. Independently reconstruct both constraints and all candidate signatures without using the primary evaluator.
5. Recheck the polynomial difference between top160 and128 on every three-bit input. The expected difference is the indicator of101, V_left*(1+V_center)*V_right over GF(2). On the encoded image it vanishes. Different physical program words mean different full physical encodings even when the encoded data image is identical.

## Physical and endogenous-program controls

Use the unchanged H_2 interpreter and existing four-row masked layout. Active bottom(32,60), top(q,240), q in{128,160}, guards(204,240) and zero guard data. Both candidates use the same resource budget. Run in hold mode for all source words on widths4 and6, ticks0..8; compare all physical symbols with independent native execution and fresh source preparation. Count implied exterior blanks through the absorbing-blank law, separately from explicit stored-symbol comparisons.

Off-image control: U=1 everywhere, V=1 except V_0=0. This is not a prepared Rule32 pair because its top neighborhood101 is forbidden. Run both programs on width21 through8 ticks, comparing complete physical and native fields. Width21 leaves the defect's radius-eight causal cone unwrapped. Save the top disagreement positions at every tick. The undamaged pair(1,1) is the encoded image of an alternating infinite source; do not require a width21 source preimage.

Endogenous neutral-transport control: at each top site use word128+32*lambda_x, with arbitrary binary lambda. Select the existing state-gated whole-program copying mode. Independently update lambda'_x=lambda_(x-1) if old V_x=1, else lambda_x. All other rows' programs are uniform within their rows and remain unchanged. Exhaust all16 source states and16 lambda fields on width4, ticks0..4; compare complete physical symbols, prepared Rule32 data, and the evolving lambda field. Record actual instruction changes. This explicitly tests a potentially neutral instruction degree of freedom; it does not assume semantic program revision has been achieved.

## Provenance and limits

Commit protocol and implementation before evaluation. Retain corrections and deviations instead of editing the protocol. Save canonical JSON with deterministic hashes, forced maps, witness groups, syntax counts, physical traces and transport counts; CI must reproduce it exactly.

Keep the parallel Erased Distinctions workstream intact. No Class-IV score, arithmetic-prime correspondence, spontaneous role formation, unique dimensional constructor, minimal-dimensionality theorem, or novelty claim is tested. The user clarified that existing guarded constructions remain useful instruments; their encoded subsets should not be mistaken for independently selected natural structure.
