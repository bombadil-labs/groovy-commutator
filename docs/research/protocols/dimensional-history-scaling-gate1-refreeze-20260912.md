# Gate-1 refreeze: dimensional history scaling product-law control — 2026-09-12

**Status:** superseding protocol-only correction after independent Gate-1 rejection. Nothing in this refreeze authorizes implementation or evaluation.  
**Program:** *Dimensional Closure and the Commutator Lift*.  
**Parent protocol:** `docs/research/protocols/dimensional-history-scaling-20260912.md`.  
**Rejected head:** `eb992ce8d29a864b4ecf3ba94ced6adda0a22e74`.  
**Independent reviewer:** Claude Code / Fable 5.1.  

The parent protocol attempted to test the user's open conjecture `h_min(d)=d` using the accepted ordered-axis Rule-90 constructor and a codimension-one coordinate slice. Gate 1 derived the complete answer before implementation: this design is structurally blind to dimension. The original predictions and interpretation are therefore withdrawn before evaluation and replaced by the exact negative-control question below.

## 1. Corrected question

For a product-form linear ordered-axis law observed on a coordinate slice, prove and independently verify that the minimum history depth is determined by the one-dimensional transverse factor and **not** by the number of spatial dimensions.

This unit is now a negative design theorem / control. It no longer claims to test whether dimensional level generally controls history depth.

## 2. Frozen family

Retain the parent's exact finite family only as theorem controls:

- source law: ECA Rule 90;
- ordered-axis constructor `F_d = product_i (sigma_i + sigma_i^-1)` over GF(2);
- dimensions `d in {1,2,3}`;
- odd periodic side lengths `n in {5,7}`;
- observation: complete coordinate slice at newest-axis coordinate zero;
- exact history criterion `ker(H_h) subseteq ker(O F^(h+1))`;
- declared cap `H_max=8`, which is nonbinding for these controls.

No touching-strip result is counted as a datum here.

## 3. Analytic theorem frozen before implementation

Write

`R_d = GF(2)[x_1,...,x_d] / (x_i^n - 1)`.

For the newest axis define

`g = x_d + x_d^-1`

and

`A = product_{i<d} (x_i + x_i^-1)`.

Then `F_d = A g`. The coordinate slice is the `R_{d-1}`-module generated by `1`, so

`rowspan(O F_d^k) = R_{d-1} A^k g^k`.

Let `D(n)` be the degree of the minimal polynomial of `g` in `GF(2)[x]/(x^n-1)`. For odd `n`, `A` is not nilpotent. Powers `1,g,...,g^(D-1)` are linearly independent over the transverse coefficient module, while the minimal-polynomial relation for `g^D` closes the history span. Therefore

`h_min(d,n) = D(n) - 1`

for every tested `d`, and more generally for every dimension in this product-form construction under a coordinate-slice observation.

For the frozen sizes:

- `D(5)=3`, hence `h_min(d,5)=2`;
- `D(7)=4`, hence `h_min(d,7)=3`.

These values are **theorem predictions known before any run**, not empirical bets.

## 4. Required independent controls if Gate 1 approves this refreeze

Implementation-only/no-result may pin an exact checker, but evaluation is a theorem regression rather than a discovery. It must include two materially independent paths:

1. packed GF(2) matrices for `F_d`, `O`, history ranks and row-span inclusion;
2. a basis-action / polynomial-ring reference that does not reuse the packed matrix powers.

The canonical control result should record, for all six `(d,n)` cells:

- rank sequence `rank(H_0)...rank(H_8)`;
- first closing depth;
- minimal-polynomial degree `D(n)` computed independently;
- equality `h_min = D(n)-1`;
- equality of `h_min` across dimensions at fixed `n`.

Any discrepancy is an implementation/proof blocker, not an alternative scientific outcome.

## 5. What this unit establishes if verified

Allowed conclusion:

> Product-form linear ordered-axis laws under coordinate-slice observation are dimension-blind for this history-sufficiency measure; the apparent depth is controlled by the one-dimensional transverse polynomial and finite ring arithmetic.

This explains why the rejected design cannot test `h_min(d)=d` and records a reusable null for future protocol design.

It does **not** refute the user's broader history-to-geometry conjecture. A future discriminating test must break at least one ingredient responsible for this theorem, for example the product factorization of the law or the coordinate-slice module structure of the observer. Such a replacement is a separate protocol and is not chosen post hoc inside this unit.

## 6. Mandatory nonclaims

Do not infer intrinsic dimension, a universal relation between history and space, an infinite-lattice theorem beyond the stated algebra, self-assembly, endogenous control, resonance/self-recognition, physics/metaphysics, Class IV, universality, spacetime emergence, or any prime/`8n+1` connection.

The dependence of `D(n)` on ring arithmetic is finite-period algebra, not evidence that prime numbers determine physical dimension.

## 7. Renewed Gate-1 questions

The independent reviewer should verify before any implementation:

1. Is the theorem statement and module argument correct as written?
2. Is `A` non-nilpotent for the frozen odd sizes sufficient for the lower-bound step?
3. Are `D(5)=3` and `D(7)=4` correct for `g=x+x^-1` modulo `x^n-1`?
4. Does the conclusion genuinely apply for every `d` in this product-form constructor with a coordinate slice?
5. Are the proposed independent controls non-circular?
6. Is it scientifically useful to preserve this rejected design as a negative-control theorem rather than silently replacing it?
7. Are the nonclaims strong enough to prevent this from being reported as evidence against history/dimension scaling in other representations?

This refreeze materially changes the original predictions and interpretation. **Renewed exact-head Gate 1 is required.**