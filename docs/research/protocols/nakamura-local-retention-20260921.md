# Frozen follow-up: locality of the eight surviving quotients

Authored by Codex (OpenAI), 2026-09-21. Reviewed by: none. Run and solo
integration authorized by Myk's current-session instruction to set up and
continue the work solo, as recorded in the
[first protocol](nakamura-retention-20260921.md). No independent review claimed.
Status at freeze: unrun.

## Why this one follow-up is justified

The completed first audit found 56 Rule-110 quotient candidates refuted and
eight surviving: keep masks 56 through 63. Those are measured outcomes, not
predictions for this unit. The check grouped entire observed ring fields, a
necessary condition for any factor. It did not certify the promised radius-one
readout/update cost. Seven of the eight candidates are nontrivial and discard
some previous bits when current=0. This creates one concrete remaining risk:
information discarded locally may only be recoverable by looking farther away.

Check the locality contract before calling those survivors compression
successes. No larger ring, longer trajectory, extra source rule or change of
simulator is needed. This is a separate frozen unit; the first protocol and its
result remain unchanged. The present protocol explicitly ends the numerical
search after this check, even if candidates survive.

## Domain and exact question

Reuse exactly the first unit's reachable width-five prefixes, source Rule 110,
and quotient masks 56..63. Keep the Rule-204 identity control. All initialization,
update and phase conventions remain those of the first protocol. For each
candidate Q and site i, group prefixes by the ordered radius-one observation
(Q(s[i-1]), Q(s[i]), Q(s[i+1])). A translation-invariant radius-one update exists
only if every occurrence of one such observed neighbourhood has the same
enabledness and, when enabled, the same successor centre label Q(U_i(s)[i]).

A conflicting pair is an exact obstruction under this interface, even when
the full observed configurations differ outside that neighbourhood. Save the
two initial sources, phases, prefix update lists and the respective centre
positions. Positions may differ: shift-equivariance lets either be moved to
the origin. Replay both prefixes and check the three labels and output labels.
Also report whether the conflict is visible in the current bit, so the
scientific interpretation does not confuse hidden-state bookkeeping with
failure of the physical bit readout.

The identity quotient must pass; all eight identity-rule controls should pass.
No expected outcome is registered for the seven nontrivial Rule-110 candidates.
Finite survival is not an all-domain existence certificate. Any negative lower
bound is restricted to pointwise quotients preserving the exact current/age
interface, radius and cadence; it is not a bound on all asynchronous simulators.

## Execution and stop

One script importing the first runner's prefix/transition definitions, one
canonical JSON, a 60-second canonical-run limit. Hash both scripts, both
protocols, the first result and the CA primitive. Record the shared-code
dependency explicitly; replay is self-verification, not independent review.
Test all eight candidates on both rules and keep every outcome. End the
numerical investigation here. If something survives, the handoff names the
unproved congruence/locality question without proposing an automatic horizon
expansion. If all nontrivial candidates fail, close this direct-compression
route and explain why established alternative protocols remain possible.
