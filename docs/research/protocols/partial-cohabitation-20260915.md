# Partial-rule cohabitation and default completions

Authored by: Codex (OpenAI), Myk's dimensional-lift session, 2026-09-15.
Protocol review: none at freeze; run authorized by Myk 2026-09-15 under standing Gate 1 approval and the request to explore unspecified lifted entries. Evaluation precedes independent final review. Local freeze and one final publication batch replace intermediate protocol PRs at Myk's request.

## Question and domain

Does selecting a default completion erase genuine compatibility between the current six-field partial rules? Reuse the preserved uniform-six-field archive (SHA-256 `766e4db7083fbdb551bc4aee66abc554079c5d118905f6d65aa5e5372c9418d1`). Analyze all 256 ECA roots at D2, complete source rings 7 and 8, fixed physical radius [3,2]. Do not construct new lifts or trajectories. These are finite source families, not exhaustive infinite-line constraints.

The older cohabitation implementation and its claimed 39%/48-clique results have not been found in inspected repository paths. The supplied note identifies a separate Claude workspace. This is a fresh audit of the current cache, not a reproduction or correction of those historical numbers. GitHub code search returned incomplete results; absence from those searches is not proof of absence from all branches or private workspaces.

## Definitions, fixed before execution

Represent each local flip rule as two sets K0 and K1 of actual physical 35-bit neighborhoods. Every other key is unspecified. Keys are row-major dy=-3..3, dx=-2..2, first bit most significant; never compare independent cache intern IDs. Forced zeros are commitments. The decoder is a separate interpreter and does not enter native-rule cohabitation.

For each unordered distinct root pair, report shared forced zeros, shared forced ones, conflicting keys, and total shared support. Compatibility means no conflicting key. If compatible, admitting Q into P's completion family costs |K_Q minus K_P| additional pinned bits; otherwise there is no shared completion. Count both vacuous (no shared keys) and occupied compatibility explicitly.

Compare four individually completed tables, using the same policy for both roots: no flip (mask 0), flip (mask 1), output zero (mask=center), and output one (mask=1 XOR center). Equality of completed tables is a deliberately different predicate, not the correct cohabitation definition. Evaluate equality over the union of forced domains, since both tables use the identical policy outside it. Count false conflicts relative to partial compatibility. For every policy the resulting equality relation must be a subset of partial compatibility.

First compare widths separately (primary width 7, declared sensitivity width 8). Then compare each root's width-7 and width-8 assignments. If all 256 unions are consistent, also compute the pooled-width pair graph; otherwise retain the cross-width witnesses and do not compute an invalid pooled graph. A pooled edge must occur in both individual width graphs, but cross-width constraints may remove additional edges. Width agreement does not prove full-shift agreement.

Report all 256 per-root degrees and mean shared/conflicting support, full pair records, default-equality counts, width changes and explicit witnesses. Describe the 88 symmetry representatives using the archived label convention; report 54/110 and disputed 41/106 separately. No classifier, class threshold, ranking optimization, maximum-clique search or trajectory statistic is introduced. Report output-complement and Boolean-conjugacy pairs separately, with no transfer of older recipe identities assumed.

## Predictions and checks

- P1: all archived D2 assignments reproduce their stored invariant-family successor, and physical keys match direct scalar extraction on bounded controls.
- P2: all four completed-equality relations are subsets of partial compatibility, at every computed domain.
- P3: no-flip completion discards at least one compatible pair. Preserve failure if it discards none.
- P4: each root's two-width constraints are jointly consistent. If not, report the conflicts and skip pooling.

Other counts are descriptive. Verify the count identities, symmetry, decoder recovery of archived source labels, self-compatibility, and witnesses. Independently review source, evidence and interpretation after execution. Budget: five minutes wall time, 1 GiB memory; scientific execution and any necessary independent replay occur outside Actions. Automatic CI checks source/input integrity and a small semantic control only. Preserve inputs' archive/member hashes, packed physical tables, pair matrices, exact source freeze, runtime and result provenance.
