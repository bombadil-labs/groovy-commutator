# Frozen next experiment: physically stored Rule32 cap and edit contract

Date: 2026-09-10. Status: planned, not executed. Candidate selection is post-census; the logical Rule32 identities were evaluated before this physical protocol was frozen.

## Fixed candidate and domain

Use the existing five-symbol, radius-nine H_2 table-chain interpreter, scale9, hold-program mode. No interpreter modification or new role type. Native source law for the semantic test is fixed homogeneous Rule32 on the full infinite horizontal lattice. Prepare D_32(S)=A_0(S) and A_1(S), each of finite horizontal radius at most2.

Occupied logical rows are {-1,0,1,2}. Row0 carries (U=A_0(S), program(32,60)); row1 carries (V=A_1(S), program(128,240)). Rows-1 and2 are zero-data guards with program(204,240). Outside the four complete macrocell layers is blank. Guard data freeze by missing outward input. Row0 uses F_32(U) xor V; row1 uses F_128(V) and ignores vertical inputs. All stored programs are held.

The desired identity is H_2 J E(S)=J E(F_32(S)) on the complete physical field, for every S. Prove locally, then induct. Report program storage, data storage, occupied sites (4*17=68 per horizontal source site), physical transverse span36, preparation radius, physical radius and tick cadence. Do not claim global storage optimality or that the arbitrary physical state space is a correction representation.

## Audits committed before execution

Commit implementation before running this protocol. Record any corrections/deviations without editing this frozen document.

1. Recheck all128 seven-bit source windows for the semantic two-row identities using a direct evaluator independent of the physical interpreter. Reuse the frozen census only as a comparison, not as the physical update implementation.
2. Exhaust all32 local five-bit native data neighborhoods for each of the two active program tuples, comparing the physical microcell rule against independently evaluated native tables. Verify guard retention and program/blank retention as well as data output.
3. Complete periodic-horizontal physical field controls: widths3 and5, every source initial word, ticks0..40. Compare every occupied and blank physical symbol against native two-row evolution and against freshly prepared Rule32 source evolution. These periodic replays are finite controls; the local proof supplies the infinite-lattice theorem.
4. Retain the failed zero-cap control on the same prepared two-row initial data: replace top program by(32,240). Record the first mismatch in ascending width/source/time order, if one occurs; if not, report that fact. Do not silently replace the candidate or lengthen the horizon.

## Instruction edits and repair: distinct contracts

At tick0 and horizontal site0, separately flip each of the32 active instruction bits (16 in each of two rows), and each of the two active data bits, for all40 source words across widths3 and5. Run each of these34 edits through8 ticks.

Contract A, required: a one-bit native program/data edit is exactly one physical P/D symbol edit; physical and independently executed edited native fields agree at every subsequent tested tick. Edits change the represented native program, not the ambient interpreter. Compare all symbols, not just decoded row0. Guards, occupancy and tags are not edited in this experiment.

Contract B, measured without a success expectation: for each edited run record (i) equality of the two active data rows with the undamaged data trajectory, and (ii) membership of the active pair in the fixed-Rule32 K_1 image of the same finite ring, at each tick0..8. These are different properties; periodic-image membership is not full-shift image membership. Programs can remain changed even when data recover. Report program recovery separately; held edited program bits cannot revert under this hold policy.

A cap instruction edit is not automatically an edit of the original homogeneous Rule32 law. Do not interpret Contract A as preserving correction semantics or as autonomous repair. Fixed-rule initial preparation supplies the semantic witness; edits may leave it. Any proposed re-preparation, changed source law, dynamic repair, or alternative cap must be frozen as a subsequent rescue before evaluation.

## Decision and limits

If the full-field identity passes, record an indefinitely closed physically programmed two-row correction example, with its prepared resources and scoped edit behavior. If it fails, retain the first counterexample and diagnose the precise contract. No inference about all larger rule spaces, all cap architectures, Class IV, universal simulation, or novelty is authorized by these tests.
