# Checkpoints: Dynamics of Erased Distinctions

No inline `CLAUDE.md` checkpoints were ever recorded for this Program; its
running account has always been the Program page itself, together with the
shared closure account (`docs/research/2026-09-10-shared-closure-account.md`)
and the knowledge entries registered in PR #71. Start new checkpoints here
from the next completed unit onward.

Each section is a Program-scoped checkpoint: a dated statement of state that adds no evidence. The most recent unit is first except where an orientation
section is deliberately placed at the top. Each records what a unit completed,
what is frozen and unrun, and what not to infer. Add a new checkpoint at the top
when a substantial unit completes; keep the bounded-claim style. Text moved from
`CLAUDE.md` on 2026-09-10 is verbatim.

Program page: `docs/research/2026-09-08-dynamics-of-erased-distinctions.md`

## Checkpoint 2026-09-22: readout-cost design complete

The [cost note](../2026-09-22-groovy-readout-cost.md) derives a three-track
cache with the same four-bit marked query as the jet. No phase-free binary
dominance or empirical speedup is inferred. No broad benchmark is queued;
the [drive handoff](research-drive.md) records the next consumer decision.

## Checkpoint 2026-09-21: executable cases and causal-retention unit complete

The [three cases](../representation-case-studies.md) now have a fast verifier.
The [causal-retention account](../2026-09-21-causal-retention.md) records the
restricted Rule-110 twelve-state lower bound, both frozen audits, and the
whole-field/locality distinction. This adds no asynchronous property to the
dimensional lift and no Class-IV claim. The numerical search has stopped.
Codex authored and self-verified under Myk's explicit solo authorization;
independent review is not claimed. Follow [the handoff](../NEXT_TASK.md) for
verification and integration status, then require a concrete cost advantage
before proposing another unit. Earlier queued work remains historical.

## Checkpoint 2026-09-21: research reset

The source-recoder material from PR #231 is integrated and parked at 14/22, with eight unresolved and no recovered certificate. The prior 22/22 scientific completion condition remains unmet; scheduling is paused regardless. Current work is the three representation case studies.

See [current direction](../START_HERE.md), [reset integration record](../2026-09-21-research-reset.md),
and [held-structures correction](../2026-09-21-held-structures-account.md).
This is scheduling/integration state, not new independent scientific review.

## 2026-09-14 — Phase-splice source-recoder frontier integrated; recovery still at 0/8

**Authored by:** Claude Code, Opus 5, session `session_01SobHxxYcFURkku6CFKrhSZ`. **Reviewed by:** none at the time of writing; review requested on the gathering PR.

**What this unit completed.** It integrated the orphaned phase-splice /
bounded source-recoder thread onto `main` — the 2026-09-10 terminus/synthesis
note, both frozen protocols, the recovery driver and aggregates, the parent
canonical audit and the eight resumable per-seed checkpoints — and then ran the
frozen exact-recovery protocol further. It also closed a reproducibility gap:
`python-sat` is now a declared `research-sat` optional dependency, and the three
result files of this thread are registered in the fast integrity tier.

**What it did not complete.** The recovery. The tally is unchanged from
2026-09-10: **14/22** frontier seed languages exactly classified, **0/8**
recovery seeds recovered, **0** certificates. A campaign on 2026-09-14 added
about 9.3 recorded portfolio solver-hours, 721 exact partition splits and 715
closed cylinders against 2,896 new timeouts, and decided **no** structural
tuple. Cumulative across both campaigns: ~45.5 solver-hours, 3,501 splits,
4,041 closed cylinders, and **zero** fully assigned direct-evaluation leaves
ever reached. All five protocol controls reproduced before interpretation. The
numbers live in
[note 039](../2026-09-14-bounded-source-recoder-recovery-status.md); this
checkpoint only cites them.

**Therefore the Program is NOT marked complete/dormant.** The terminus note's
closure recommendation is conditional on 22/22 and that condition is unmet.
Marking it dormant now would convert an unfinished computation into a
conclusion.

**Review status is retrospective, and that is a real gap.** This thread predates
the gathering-branch and cross-model-review convention agreed with Myk on
2026-09-11. The parent bounded-synthesis experiment and the exact-recovery
protocol were both frozen and evaluated with **no** gate-1 review of any kind,
and the draft PR carrying them received zero review comments. Review is
therefore requested retrospectively under `AGENTS.md`'s correction path. No
document in this unit backdates review or implies it preceded evaluation.

**What is frozen and unrun.** Nothing new was frozen. The recovery protocol
stays frozen exactly as written: the same 22 seed languages, `m <= 4`,
`t <= 6`, the same structural order, the same deterministic adaptive
rail-selection grammar, the same universal equality criterion, the same
operational 10-second solver slice and split order. The remaining work is to
resume the committed checkpoints.

**What not to infer.**

- Not that the eight undecided seeds have no bounded recoder. `pending` is
  scheduling state; it is neither a censoring nor a negative.
- Not that these instances are intrinsically hard. This Program's own
  phase-splice lesson is that a wall in one exact encoding said nothing about
  the dynamics.
- Not that the selector-state ladder may be resumed. Enlarging `m` or `t`, or
  inventing another selector geometry, is a new research question needing its
  own frozen protocol.
- Not that the two-switch rail-selector rung is an exact classification. It is
  retained as a diagnostic only.
- Not that the frozen empowerment census or the online suffix learner are
  affected. They are separate work and were not touched.
