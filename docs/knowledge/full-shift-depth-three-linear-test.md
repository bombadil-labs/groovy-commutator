# Decide full-shift refinement depth at most three under the two linear observations 90 and 150

Completed 2026-09-12. Frozen protocol gate-1 reviewed by an independent collaborating agent (OpenAI GPT-6 Astra Pro) before implementation, with no required change to any frozen object, definition or prediction. Verifier committed on the gathering branch (a recorded process deviation: direct commit instead of a sub-PR); provenance corrected in a follow-up sub-PR. Single canonical run of the unchanged sixteenth-unit sparse pair-graph machinery on 90, 150 and 90's directly computed conjugate 165. Held: O1 (theorem and cross-implementation consistency controls against the seventh unit's `h_star` table), O2 (all twelve pen-decided rules confirmed outside `FS³`; of the rest, 14 of 18 under 90 and 24 of 24 under 150 have full-shift depth exactly three), O4 (ladder reported; class `c_FS = 3` nonempty under both observations), O5 (18 depth-two witness pairs replay), O6 (complement transport and reflection invariance). Failed: O3, the rings-3-to-14 depth-three gap is nonempty under 90 (4 rules) but empty under 150. No deviation beyond the recorded direct-commit routing. Final review pending gate 2. See the [full-shift-depth-three-linear note](../research/2026-09-12-full-shift-depth-three-linear.md).
