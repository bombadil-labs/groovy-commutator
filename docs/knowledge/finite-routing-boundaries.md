# Explicit occupancy permits finite recursive routing boundaries

Under the unchanged five-symbol, radius-nine editable-routing interpreter, one complete guard macrocell layer on each side of a source plane suffices. The guards remain fixed because their outward required input is absent. All more distant cells are absorbing B.

The native source family explicitly includes a fixed occupied-site mask Omega, complete programs/data at occupied sites, and entirely blank missing macrocells. Data with a missing neighbor retain their value; programs copy only from present left neighbors. The physical rule realizes this logical update exactly.

The finite lift changes the mask to Omega times {-1,0,1}, appends table172 centrally, and supplies constant guard programs with data1/0. Exact evolution and inherited one-cell payload edits commute in both hold and copy modes. Arbitrary masks make the construction recursively typed through every finite dimension, including previously edited guard fields.

Each added axis occupies three logical layers, or 27 physical coordinate positions for complete macrocells. After k lifts from dimension d, there are 3^k[8(d+k)+1] occupied physical sites per initial occupied logical site. Infinite sources still have infinite total support.

The [note](../research/2026-09-10-finite-routing-boundaries.md) proves the identities and retains failures of zero-filling absent cells and omitting a guard. The frozen audit passes 26,619 assertions.

This is a missing-input boundary mechanism, not self-organization or an arbitrary minimum-width theorem. Occupancy edits and tag repair remain outside the action contract.
