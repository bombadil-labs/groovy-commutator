# A finite strip encounter escapes every fixed-height band

Under the unchanged 2D interpreter, adjacent pulses a_1=1 and b_0=1 with all
other logical bits zero launch an indefinitely widening perturbation of the
alternating background. Their initial physical difference is four cells.

For every fine tick t>=4, the unique uppermost and lowermost changed cells
are at (y,x)=(3-t,t) and (t,3-t). An exact extreme-row update identity proves
this by induction from the verified tick-four configuration. The vertical
span is therefore 2t-2 rows, without a bound.

No exact encoding confined to a fixed-height horizontal band against this
background can contain the entire orbit, even if the band translates as a
whole. This does not exclude symbolic compression, growing-support encodings,
or special input families. The growing wake is not an isolated glider and
does not demonstrate a logical gate. See the
[finite witness and all-time proof](../research/2026-09-08-interface-state.md).
