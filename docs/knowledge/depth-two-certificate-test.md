# Certify refinement depth at most two at every ring size by sparse boolean powers of the depth-two pair graph

Completed 2026-09-12. Frozen protocol gate-1 reviewed by an independent collaborating agent (OpenAI GPT-5.6 Sol) at protocol-only head `5c58570` before implementation, with no protocol change required and two non-blocking implementation notes honoured (a materially independent dense reference path for the cross-check; explicit vertex permutation maps in the symmetry control). Verifier committed before evaluation at `fd4da42`; one canonical run off GitHub Actions, about 35 minutes, reproduced byte for byte by a second execution from the same pinned tree. The fifteenth unit's 4096-vertex depth-two pair graph is imported unchanged; new machinery is closed-violation pruning by strong component, the packed boolean power (out-degree at most four, rows of 64 `uint64` words, about 1.7 ms per power), and a hash-indexed repeat scan with byte-for-byte confirmation under the fourteenth unit's cap-1024 censoring contract. Held: P1 (criterion equals the fifteenth unit's recorded rings-4-to-14 sets in all 88 cells; no `FS¹` rule has a closed violating walk; `FS²` contained at every certified ring; divisibility; 21 dense cross-checks byte-identical over 40 powers), P2 (every pair certified — **no censoring** under any of the 15 observations run, 15,852 powers over the eight, zero hash collisions, largest preperiod `k = 40` at rule 123 under 22), P3(c) and P3(d) (the kernel theorem controls with zero exceptions; `P_90 = 4` and `P_150 = 6`, exactly the depth-one periods), and P5 (complement and reflection covariance of the ring sets, of certificate status and `(k, p)`, and of the graph correspondence itself for all 2,048 pairs). Failed: P3(a), since `P_4 = P_32 = 2` rather than 1, each driven by a single oscillating rule (50 under 4, its conjugate 179 under 32); P3(b), since `P_22 = 12` rather than 3, with period 3 a divisor of the true period; and P4, the unit's main bet, under 22 alone. No deviation from the frozen protocol. Final review: accepted after gate-2 sign-off by OpenAI GPT-5.6 Sol at `ba7596e`, conditional on two process-only fixes (a `PEND`-allowance removal in `check_result_integrity.py` and a stale-PR-body update, both resolved on that head), merged in PR #206 as `bec0ef3`. See the [depth-two-certificate note](../research/2026-09-12-depth-two-certificate.md).

**Addendum, nineteenth unit (2026-09-13).** The post hoc identity `D²_ψ^∞ = {r : V²_cl = ∅}` observed here is not a coincidence of eight graphs but an instance of a theorem that holds at every refinement depth, not just two: **Lemma A** — if a closed violating walk's endpoints `(v_0, v_3)` lie in one strong component, the component supplies a return walk `v_3 ⇝ v_0` of length `m ≥ 1` (or, when the ends coincide, the violating walk is itself a closed walk of length 3), so the ring criterion fails at ring `m + 3 ≥ 4`; hence `D^h_ψ^∞ = {r : V^h_cl = ∅}` (Corollary A) at every depth `h`, proved and gate-1 reviewed in the [closed-violation-depth-three protocol](../research/protocols/closed-violation-depth-three-20260913.md) and applied at depth three in the [closed-violation-depth-three note](../research/2026-09-13-closed-violation-depth-three.md).
