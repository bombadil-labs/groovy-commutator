# Loop sectors persist under self-dual gradient dynamics

The [loop checkpoint](../research/2026-09-10-gradient-loop-invariants.md) proves that a binary local gradient factor with constant complement response kappa sends periodic wrapping-loop vector h to kappa*h. For the eight nonconstant self-dual axial sources, h is conserved in every dimension and for all times. The two constant controls erase it. Gradient replication appends zero while preserving inherited loop bits.

The frozen implementation agrees with an independent twisted-potential reference on 42,880 base macro updates in 1D/2D/3D and 1,920 interface ticks. All flat periodic ensembles in the declared shapes are covered. The all-dimensional statement follows from translation covariance and complement symmetry, not extrapolation from those small lattices.

Local flatness and existence of a periodic binary potential differ: nonzero loop sectors have twisted potentials on the infinite cover. They use valid local gradient neighborhoods, requiring no arbitrary curl-defect dynamics. These invariants depend on periodic topology; they do not characterize all conserved quantities, intrinsic dimensional ancestry, or controllability. The [next intervention protocol](../research/protocols/gradient-intervention-costs-20260910.md) is frozen and unrun.
