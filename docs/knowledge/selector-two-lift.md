# Two-rail routing closes exactly when the source preserves both backgrounds

A single constructor lifts a binary d-dimensional local CA F: evaluate F on the central slice, then use its output to select the positive (0) or negative (1) neighbor in the added dimension.

Encode a source field between a positive zero half-space and a negative one half-space. The complete encoding is invariant and intertwines one tick exactly if and only if F preserves both uniform configurations. This gives exactly 64 ECA sources, the even rules from 128 through 254.

Every first descendant preserves both uniform states regardless of its source. Thus every second interface passes, and successful original sources continue through all higher dimensions. Injective program inheritance follows by fixing each added neighbor pair to (0,1). Matched source bit flips remain single-site flips on the encoded interface.

The [note](../research/2026-09-09-selector-two-lift.md) contains the proof, frozen protocol, endpoint failure witnesses, and more than 21 million computational assertions. Finite-grid tests are implementation audits; the general result follows from the local proof.

This is a routing-program control. Source instructions are static parts of the law, not mutable program cells. Infinite homogeneous backgrounds and a distinguished interface are preparation resources. Active program spatialization, background repair, and dynamical-class discrimination are not established.
