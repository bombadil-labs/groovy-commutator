# Audit the locality radius of the factor on the image under five non-linear observations

Completed 2026-09-11. Frozen protocol reviewed by Codex before implementation (gate 1 at corrected revision `46d9a7c`, after one non-idempotence witness was corrected), verifier committed before the single deterministic run, CI replay with both tiers. Held: 200 and 4 are idempotent and every closed rule's factor equals the observation composed with the rule on the image, radius at most 2 (R1); the sixteen rule-200 residual rules have radius exactly 2 at every ring and every other closed rule under 200 and 4 at most 1 (R2); collapse rules radius 0 and commuters at most 1 (R4); ring independence for the idempotent observations at rings 7 to 12 (R5); reflection invariance and complement covariance to 236 and 223 (R6). Failed at one cell: R3, radius at most 2 under the non-idempotent observations, fails for rule 223 at ring 6 under 22, the ring-6-only closed rule, whose factor needs radius 3, the whole ring; every other cell is at radius 1 or less. No deviation. See the [factor-radius note](../research/2026-09-11-factor-radius.md).
