# Local correction closure depends on the coordinate budget

The [exact census](../research/2026-09-10-local-correction-caps.md) checks all256 homogeneous ECA on the full infinite binary lattice for correction and forward-observation tuples at h=0..2 and R=0..2. Of4,608 budgets,1,094 pass and3,514 retain conflicting source windows. Every pass supplies a local map on the represented image; every failure is scoped to that depth, radius and coordinate choice.

At h1/R2 the counts are K123/O120; at h2/R2 they are K94/O150. K and O have the same whole-field fibers, not equal fixed-radius patches. Increasing correction depth changes the target and can increase the needed radius; it does not destroy closure already available in a shorter tuple.

A post-census exact witness is Rule32: A_2 is the AND of the two neighbors of A_1, giving the autonomous pair U'=F_32(U) xor V, V'=F_128(V). This holds for every correctly prepared source and every time by the local identity and induction. It is a nonconstant two-row logical cap, not yet the separately frozen physical/edit audit, a storage gain, or an established novelty claim.
