# The exact two-strip composition test is complete

The frozen experiment holds the 2D law, strip encoding, horizontal alignment,
and cadence two fixed. It checks every six-bit logical input for zero through
four background rows between strips. An independent truth-set implementation
agrees on all 320 local cases, 200 cell polynomials, and every validity and
cross-influence decision.

Separated strips remain independent; adjacency usually leaves the code.
Matched-action corroboration includes 671,744 independently computed fine
fields, 139,264 coarse paired states, and 196,608 action-word states on the
declared horizontal rings with no vertical torus. An explicit buffer argument
extends independence to all positive gaps and any aligned collection.

The experiment distinguishes physical interaction from logical coupling that
preserves the representation. See the
[protocol, evidence, and limits](../research/2026-09-08-coupled-strips.md).
