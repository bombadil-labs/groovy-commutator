# Loop revision has a support cost; inherited control has a replication cost

Status: exact under the declared flat-mask action contract. The [intervention checkpoint](../research/2026-09-10-gradient-intervention-costs.md) proves that changing loop vector h on a rectangular periodic lattice costs at least, and exactly, the sum of N/n_i over changed directions in flipped stored edges. Disjoint wrapping loops give the lower bound and flat seam masks attain it. Zero-loop masks are periodic-potential gradients and cannot revise the loop sector.

The eight nonconstant self-dual sources preserve the action-selected loop vector; constants erase every edited gradient after one update. Replication into an added period m preserves edited trajectories while multiplying both changed-edge and touched-site costs by m. These are simultaneous support costs under an external one-shot action interface, not a local or endogenous controller.

The frozen audit covers all flat fields and masks on (4), (2,2), and (2,2,2), 31,495,680 endpoint queries, and 38,400 interface endpoint checks without discrepancies. It saves every mask, cost minimum, witness, and budget-count distribution with independent evaluator checksums. At target edge budget eight, inherited (2,2) actions yield 27 full-field outcomes versus 126 under native (2,2,2) actions for every nonconstant source and declared horizon. Native actions add choices without lowering the minimum cost of a specified inherited loop change.

The period-two nonlinear dynamics are bijective on each loop sector, so their full-field counts primarily describe action geometry; they do not establish general reversibility. The four-site ring shows within-sector loss while conserving loop bits. Only for the specified known-state deterministic hard-budget channel does capacity equal log2 of the integer reachable-outcome count. Uncertain states, sequential actuation, feedback, restoration, and endogenous organization remain separate questions.
