# A useful distinction can depend on distinctions already represented

For fixed target future C and present refinement Z' of Z, the reduction H(C|Z)-H(C|Z') equals I(C;Z'|Z). Its value can depend on the current encoder; diminishing returns is not automatic.

[Research028](../research/2026-09-08-block3-representation-design.md) exhausts all ECA rules, canonical binary three-cell targets and their full refinement intervals at n=12, cadence3. Greedy repair is globally cheapest in 30,852 of 30,856 nonclosed cases. The four failures are in the Rule24/231 conjugacy family, each with one bit of regret at n=12.

In the independently checked first example, the cheaper path begins with an essentially zero-gain split that increases a later split's normalized gain by about 13.26 times. A separately frozen n=15 test preserves the failure and amplification, with 1.25 bits of regret. Numerical zero and equality statements retain the source's tolerances; no all-size identity follows.

This limits the two-cell greedy result without replacing it. It motivates preserving possible complementarity, but provides no specific coarsening timer, adaptive policy or optimality guarantee for issue64's conservative suffix learner.
