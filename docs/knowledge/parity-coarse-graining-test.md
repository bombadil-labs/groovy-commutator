# Audit neighbor-parity coarse-graining against closure, the factor, the commutator, and the refinement chain

Completed 2026-09-11. Frozen protocol reviewed by Codex before implementation (gate 1 at revision `9997462`), verifier committed before the single deterministic run, CI replay. All five predictions held: closure exactly on the 32 constant-complement-response rules for `n ≥ 6` (and, unpredicted, at `n = 4, 5`); elementary factors matching the frozen formula with rule 90 fixed; commutator covariance on rings 8 and 10; `h_* = 0` exactly on the closed set at every ring; the closed set equals the first audit's self-dual set plus the complement-invariant rules. One implementation deviation: the first run failed at the JSON write on a numpy boolean, a serializer was added and committed, and the recorded run is the only surviving output. See the [parity coarse-graining note](../research/2026-09-11-parity-coarse-graining.md).
