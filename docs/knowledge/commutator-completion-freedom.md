# A partial beam determines a family of commutators

For an invariant beam with partial native flip table P, write D(Y)=Y XOR H(Y) and T(Y)=Y XOR H^2(Y). The ambient commutator is exactly G_H(Y)=T(Y) XOR delta_H(D(Y)). Every forced neighborhood in D(Y) gives a fixed G bit; each distinct queried unforced neighborhood contributes one shared Boolean variable. Under unrestricted local Boolean completions, U such keys give exactly 2^U joint commutator fields.

Different completions can therefore change G without changing the native evolution or six-field lift on the beam. This is verified at D2 even against the complete arbitrary-input first-lift constraint table. D3 and D4 counts remain relative to the audited finite source families. The raw commutator of a default completion cannot automatically be treated as intrinsic to the partial-rule beam.

Individual ambiguous cells can still have fixed parity relations when they share a physical query. The resulting affine family is fully determined by the declared partial table and source configuration. Its incidence includes ordinary translation repetitions; neither its size nor its repeated variables establish Class IV specificity or invariance under recoding.

The two-step carrier T remains fixed. It is not itself identical to native G. Transporting the entire XOR operation through a fixed injective encoding defines a different, completion-independent conjugate of root G; this must be distinguished from ambient cellwise XOR.

Source: [completion-independence audit](../research/2026-09-15-commutator-completion.md). Status: exact algebra for the declared completion contract, with exact finite source counts and complete first-lift membership at D2. Universal higher-input membership and a class discriminator remain open.
