# Two lags restore full-field determinism for a small subset of the frozen touching-strip representations

On the accepted `n=6,7` touching-strip reachable family, 183 of 192 complete-history cells remain globally conflicting, but all nine passes occur at `D2,h=2`. Full six-bit `P15` passes globally on both rings even though its accepted local factors conflict through `R=2`; therefore its two-lag representation retains enough information somewhere in the complete ring field while the bounded locality scale remains unresolved. Ring 6 passes masks 11, 13 and 15; ring 7 passes 3, 5, 7, 11, 13 and 15. This is finite-ring single-valuedness only, not a bounded-radius, translation-equivariant or infinite-lattice factor. See the [global interface-history note](../research/2026-09-12-interface-history-global.md).
