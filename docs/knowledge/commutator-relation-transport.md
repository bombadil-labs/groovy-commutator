# Completion-independent parity does not yet identify Class IV

In the unrestricted local completion family, ambiguous native G cells sharing
one free physical query have fixed parity. Remove simultaneous translation of
the source and pointed site, then compare entire centered lifted states to
remove remaining spatial copies. The relation rank is the number of ambiguous
orbit classes minus the number of distinct variables.

An exact finite audit and independent physical replay cover all 256 roots for
the longitudinal census and 13 fixed roots for the full spatial quotient and
seven child readouts. On source width seven, 54 and 110 have zero ambiguous
relation rank at D3/D4. On width eight they retain relations, but controls
18 and 126 have the same respective higher-floor ranks and survival counts.
Color-complemented representatives have different signatures. These counts
therefore do not provide a Class-IV criterion invariant under bit convention.

For 54/110 at width-seven child floors D3/D4, separate all-free sibling blocks
use disjoint free variables. Any fixed nonconstant Boolean readout confined
to each such six-bit block can vary independently, so cannot restore fixed
cross-block parity. This conditional obstruction does not concern fixed or
mixed blocks, overlapping spatial readouts, constrained completions, larger
widths, or universal lift transport.

The relational object remains exact within its completion contract. The next
open explanatory question is what generates the surviving difference-query
collisions and their width/representation dependence. No additional experiment
is scheduled by this knowledge entry.

Source: [relation-transport audit](../research/2026-09-15-commutator-relations.md).
Status: exact at the specified finite contracts; no intrinsic Class-IV claim.
