# The natural touching-strip interface state fails bounded local factorization

**Status: exact bounded finding.** The [interface-factor protocol](../research/protocols/interface-factor-20260911.md) and [result note](../research/2026-09-11-interface-factor.md) exhaust the declared adjacent-strip reachable family on rings 6 and 7.

The retained state is `(A,B)` plus any subset of four natural interface-correction bits `E0..E3`. All 16 frozen masks fail autonomous local factorization at every tested longitudinal radius `R=0,1,2` on the pooled primary horizon `t=0..3`; the full six-bit state also fails every tested radius through stress horizon `t=0..6`.

The coordinate bijection and independent physical replay controls pass. For the full state at `R=1` and `R=2`, canonical equal-symbolic-neighborhood conflicts replay to differing physical cells outside the retained four-row band inside the next-step causal patch. The `R=2` primary witness has equal five-symbol neighborhoods but next symbols `0` and `24`.

This is a representation- and budget-specific obstruction. It does not rule out larger radii, history, moving/support-tracking state, other coordinates, nonlocal factors, special source families, arbitrary-width grids, or recursive dimensional lifting.
