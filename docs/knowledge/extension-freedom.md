# Encoded dynamics does not uniquely determine its ambient extension

The [extension-freedom census](../research/2026-09-10-extension-freedom.md) fixes the full-shift Rule32 data image K_1=(A_0,A_1). Its radius-one pair neighborhoods occupy15 of64 inputs. Requiring the correct two-bit next output leaves98 truth-table bits free, hence2^98 local pair maps. This broad logical grammar is not wholly executable by the stored table-chain interpreter.

Within that interpreter, with bottom(32,60) and positive top guard0 fixed,128 of65,536 top tuples pass. They give four distinct effective functions,32 syntax aliases each. Restricting the top to an ECA gives exactly128 and160. Both preserve every prepared data trajectory, but differ on the forbidden top pattern101. Their polynomial difference vanishes on the encoded image.

The same off-image defect produces different expanding zero patterns under128 and160, at identical radius and stored-program budget. These are different physical program encodings of the same data image. The result preserves the earlier cap theorem while ruling out uniqueness from encoded compatibility alone. No intrinsic-dimensionality, arithmetic-prime or novelty claim follows.
