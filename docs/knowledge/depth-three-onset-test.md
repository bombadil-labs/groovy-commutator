# A depth-three ring certificate by restricting to carrying components: onset 65 under observation 232, realized by exactly two rules

Completed 2026-09-14. Frozen protocol gate-1 reviewed by an independent collaborating agent (OpenAI GPT-5.6 Sol) through **two** correction rounds: round one confirmed the restriction lemma (every return walk of a closed violating walk stays inside its strong component, so the power sequence on the restricted matrix `A_U` preserves every queried ring-membership bit exactly), the inherited onset machinery, and T4's transport, and required two corrections — **B1**: the theorem-level onset lower bound `N³_ψ ≥ n_min` only holds when the observation's eventual period `P = 1`; the general bound is `N_r(P) ≥ max(4, f − P + 1)`, hence `N³_ψ ≥ max_r max(4, n_min(r) − P³_ψ + 1)`, which is what the nineteenth unit's own record for observation 150 (`P³_150 = 6`, max `n_min = 9`) already required and the frozen bet `N³_150 ≤ 7` was consistent with; **B2**: the protocol's "about four hours" runtime figure is an operational planning estimate derived from the probe's size distribution, not a proven upper bound under the frozen cap. Both applied at `563dc75`. Round two fixed one records-only wording lag (the checkpoint's earlier bullet still said "bounded by" after the protocol itself was corrected), applied at `5a557f9`. Implementation landed as a no-result sub-PR (`fd3d259`) before any evaluation. One canonical run off GitHub Actions, executed twice from the identical pinned tree in two independent git worktrees (6680.4 s and 6550.6 s), byte-identical (SHA-256 `089c92edf75707c8720e45dc3e06c791f6466b73223cbdc8f9c03910dda30184`). The eighteenth unit's sparse packed-boolean-power `(k, p)` machinery is generalized by restricting it to the union of carrying strong components (median 1,726 vertices, max 14,168, against the full 65,536-vertex matrix) rather than reused unchanged. Held: T1 in every part (the restricted method reproduces the eighteenth unit's certified depth-two sets/periods/onsets exactly for all 2,048 pairs, with the expected restricted-vs-full `(k,p)` divergence surfacing correctly while ring membership still agrees; reproduces the sixteenth/seventeenth/nineteenth units' recorded data with their hashes; agrees with unrestricted frontier reachability and full 65,536-vertex powers on a frozen sample), T2 (certificates reported), T4 (complement/reflection transport exact on every computed pair). T3's theorem background held with zero exceptions. Of T3's four bets: **(a) held exactly** — `N³_232 = 65`, realized by rules 94 and 133 alone, both `(k,p) = (69,1)`, both leaving membership at ring 9 and not returning until ring 65; **(b) failed** — the per-observation ceiling of 15 under 4 and 32 is exceeded (both land at 22, via rules 77/146 under 4 and 77/182 under 32); **(c) held** — onset minus max `n_min` is at least 6 somewhere (232, margin 52) and zero somewhere (22); **(d) held** — nothing above the ceiling of 65, nothing censored. Observation 102 is theorem-trivial (`N³_102 = 4`) and is serialized explicitly, per the nineteenth unit's own corrected convention. No deviation from the frozen protocol beyond the two gate-1 rounds. A provenance incident is recorded and resolved without loss: both canonical-run worktrees finished and wrote their results before this session's container was restarted mid-monitoring; the already-completed files' SHA-256 identity was independently reconfirmed afterward, with nothing re-run or reconstructed. Final review: gate 2 requested from an independent collaborating agent. See the [depth-three-onset note](../research/2026-09-14-depth-three-onset.md).
