# The same local selector semantics crosses the first two law/state cardinality matches, but Z1 requires role overlap

The complete binary 0D floor and the accepted ECA shared-shell selector instantiate the same overlap-permitted local multiplexer semantics, but their physical role accounting differs: Z0 attains zero address/program overlap while Z1 necessarily needs at least two overlapping roles in a nine-cell Moore patch and attains that minimum with `W` and `E` dual-role. Across the frozen 16-layout Gray family, every Z1 selector has all nine inputs essential, degree four and exactly six quartic terms; the familiar ECA mirror/complement group has identity-only intersection with the induced square-shell `D4` actions. This is an exact local architecture result, not a repeated dynamical lift or an intrinsic-dimension theorem. See the [zero-dimensional base note](../research/2026-09-12-zero-dimensional-base.md).