# Decide full-shift refinement depth at most three by sparse reachability and tabulate the depth ladder

Completed 2026-09-12. Frozen protocol reviewed by Codex before implementation (gate 1 at corrected revision `d1eeaef`, after the transport of historical sets to conjugate observations was made explicit), verifier committed and its implementation stage made green before the single canonical run from that head (121 min), CI replay with both tiers; two earlier provisional executions agree byte for byte and are process history only. Held: monotonicity, full shift implies rings 3 to 14, divisibility and `c_FS ≥ c_R` (N1); not all 24 deep rules lie in `FS³`, in fact 20 do not (N2); the rings-3-to-14 depth-three gap is nonempty under 232, 200, 22 and 4 (N3); the class of full-shift depth exactly three is nonempty under each (N4); the 24 depth-two witness pairs pass the trimmed replay (N5); complement covariance against directly computed conjugates and reflection invariance (N6). No deviation; the CI replay budget was raised to 300 minutes after the run. Gate 2 pending. See the [full-shift-depth-three note](../research/2026-09-12-full-shift-depth-three.md).
