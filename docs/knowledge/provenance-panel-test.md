# Exact spacetime provenance experiment

The protocol and implementation were committed before enumeration. Candidate sets follow visited-input constraints; direct row partitions, package engine checks, bounds and witness replays pass. No class labels are loaded. The experimental provenance JSON is a selected witness graph, separate from this semantic knowledge graph.

See [the provenance experiment](../research/2026-09-08-spacetime-provenance.md) for definitions, exact bounds, data, and limits.
