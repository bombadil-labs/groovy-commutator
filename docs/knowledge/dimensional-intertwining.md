# A lattice quotient intertwines any 1D CA with a local 2D rule

For a surjective lattice map L from Z^2 to Z, encode E(S)(z)=S(L(z)). Choose each physical dependency offset to map to its source offset. The local rules then satisfy F_2 E = E F_1 for every source configuration.

The [research checkpoint](../research/2026-09-09-dimensional-intertwining.md) proves the identity and audits all ECA local tables. For L(x,y)=x+y, 218 active ECA stencils have affine dimension two. A separate four-site term vanishes on encoded states while making the ambient stencil affinely 2D for every ECA.

These are geometric properties of the local rule, not independent two-dimensional encoded information. Encoded fields repeat each logical bit along a diagonal. Editing that bit changes infinitely many target cells, or n cells on an n-by-n torus.

This finding supplies a baseline for the dimensional program. It does not establish active mutable program inheritance or a native recursive lift. The original and stronger protocols are preserved, and the integration review reproduces all 2,160 selected periodic comparisons.
