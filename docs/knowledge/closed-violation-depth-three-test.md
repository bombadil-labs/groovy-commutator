# Decide the all-ring depth-three set, first failing rings and eventual periods from closed violating walks, with no matrix powers

Completed 2026-09-13. Frozen protocol gate-1 reviewed by an independent collaborating agent (OpenAI GPT-5.6 Sol) through **two** correction rounds and two re-freezes: round one blocked on B1 (Corollary B excludes only constant-diagonal/self-loop anchoring, not general diagonal anchoring) and B2 (the reading of a nonempty all-ring gap), approved at `aa7e67a`; a later pre-evaluation implementation review found a **mathematical error in a frozen theorem control, I1** — reflection transports the return-length residue set `R_r` as an **equal** set, not negated, because the violating walk's ends swap under `(v_0, v_3) ↦ (M(v_3), M(v_0))` and cancel the cyclic-class sign flip — and blocked evaluation, approved again at the re-frozen `3cfac14`. Implementation landed as three no-result sub-PRs before any evaluation: #219 (verifier and cost-bounded workflow, `1d5faac`), #220 (stripping wall-clock timing from the serialized JSON after a prior evaluation attempt was stopped, `092c1db`), #221 (removing `negate_residues` outright plus a self-test assertion sharp enough to catch its regression, and serializing the full eventual sets per I2, `320d3ae`). One canonical run off GitHub Actions, 70.9 minutes, reproduced byte for byte by a second execution from the same pinned tree. The sixteenth unit's `pair_graph` is imported unchanged at `h = 1, 2, 3`; new machinery is the protocol's Lemma A (the converse of the eighteenth unit's pruning lemma, giving `D^h_ψ^∞ = {r : V^h_cl = ∅}` at every depth), component periods and cyclic classes by one breadth-first level function each, the common-modulus residue sets `R_r`, and bounded breadth-first search for `n_min` with the coincident-endpoint girth case handled explicitly. 874 deep depth-three pairs, 56 `FS³` control pairs, all 2,048 pairs at each of depths one and two, and **zero matrix powers at any depth**. Held: Q1 in every part (Corollary A, `n_min`, per-rule and per-observation periods and eventual sets reproduce the fourteenth and eighteenth units' certified data with zero mismatches over 2,048 pairs, including the ring-24 `m_min = 21` control and `P²_ψ = 1, 2, 2, 1, 12, 1, 4, 6`; all 56 `FS³` controls have `V³_cl = ∅`), Q3 (consistency control clean; max `n_min` 13, 13, 13, **16**, 13, 10, 9; nothing beyond the depth-two record of 24), Q2's theorem controls and bets (a) and (b), Q4's kernel controls, Q5's Corollary B control (no oscillating rule is constant-diagonal-anchored anywhere), and Q6 entirely — complement and reflection transport with **zero mismatches over 471 pairs each** using the corrected equal-residue-set comparison, set-level transport of `D³^∞`, `Γ³` and `P³`, all 14 graph correspondences, and the first direct all-ring check of the sixteenth unit's `F_32 = F_4 ∘ ¬` deduction. Failed: Q2's bet (c), since the window overstates under **200** (five rules leaving at ring 16) and not under 22, whose window is exact at depth three; four of Q4's seven period bets (`P³_4 = P³_32 = 2` on three newly oscillating rules each rather than 1, `P³_22 = 1` so `3 ∤ P³_22`, `P³_90 = 2` rather than 4; `P³_232 = P³_200 = 1` and `P³_150 = 6` held); and Q5's anchoring bet under 232 and 200 while holding under 22. No deviation from the frozen protocol. Final review: gate 2 requested from an independent collaborating agent. See the [closed-violation-depth-three note](../research/2026-09-13-closed-violation-depth-three.md).
