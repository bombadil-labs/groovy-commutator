# Ternary transport roles have an algebraic redundancy bound

For deterministic F and binary configuration maps A, define L(A)=AF, R(A)=FA and C(A)=AF XOR FA. Starting with D=I XOR F, words of length d label a semantic side-three d-dimensional block. Its new slabs are transformed future, separately evolved role, and their correction, as proved in [Research035](../research/2026-09-09-ternary-commutator-lift.md).

Associativity gives LR=RL, and precomposition distributes through XOR to give LC=CL. Moving L symbols past the remaining R/C suffix yields at most 2^(d+1)-1 distinct maps at exact depth d. This is an algebraic statement with an explicit binary codomain.

The frozen eight-cell full-function census finds 33 rules with a finite closed cumulative role vocabulary through depth 6 and 107 saturating the exact-depth bound at every tested depth. Nonclosure within the bound is not infinite vocabulary growth. This finite substrate is not interchangeable with an intrinsic local-rule census.

The semantic address does not supply a native autonomous higher-dimensional CA or a lower bound on retained joint information. The [correction/future coordinate theorem](../research/2026-09-09-correction-future-coordinates.md) instead compares whole-field fibers under an explicit triangular recoding. Correction depth and physical spatial dimension remain separate resources.
