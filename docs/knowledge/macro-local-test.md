# Audit compiled programs on every causal window

**Completed, 2026-09-07.** For the eight declared rule pairs, enumerate the
central output of every word of length zero through eight on all 17-bit
causal windows. Equality on every window establishes equality of the local
maps and thus of the complete transformations at every positive ring width.

The [audit](../research/2026-09-07-macro-local-equivalence.md) checks all
saved finite-ring witness programs, preserves failed equalities, and repeats
the resource comparison with the stronger equivalence. An independent
shrinking-window computation verifies selected programs.

The eight-tick search bound, toy prices, and free oracle planning remain.
The follow-up was selected after the finite results, with its own protocol
committed before the new enumeration.
