# Single-pulse adjacency has an exact scattering law

For the fixed adjacent two-strip architecture, one upper and one lower single-bit Rule-90 pulse launch persistent extreme-row supports for **every integer relative displacement**. Relative position changes the exact departure time, launch time, and in one residue class the extreme-front multiplicity; it does not choose between escape and confinement for this pulse family.

A discovery census on $-64\le d\le64$ found 113 singleton launches and 16 doublet exceptions to the original singleton certificate. The extreme-row identity generalizes to any support lying entirely on the transmitting parity, resolving all 16. A fresh, non-overlapping test on $[-256,-65]\cup[65,256]$ passes the frozen arithmetic formulas on all 384 cases with dense and independent sparse implementations agreeing on every changed coordinate through first exterior change.

A finite contact-template audit then exhausts the deeper logical bits across the complete post-contact causal depth. Together with the Rule-90 edge identities, those templates cover every sign/residue class of displacement and establish the all-integer claim for this single-pulse family.

This does **not** classify arbitrary pulse shapes, phases, backgrounds, or laws. The interior wake may continue to grow even though the two boundary channels have exact outward motion. See [Research020](../research/2026-09-08-pulse-scattering.md).
