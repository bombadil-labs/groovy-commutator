# Complement symmetry makes the full gradient autonomous

For the 66 compatible ordered axial sources, the joint horizontal/vertical gradient closes exactly for 0,142,150,170,178,204,212,232,240,255. All ten have radius-one caps;0,204,255 have radius-zero caps. The four newly admitted nonlinear sources are majority functions with zero or one input negated.

Equal full gradients identify a source with its global complement. For a binary local law g, gradient closure holds precisely when g(p) XOR g(complement p) is constant across local patches. Necessity follows by placing different complement responses in disjoint windows. For the nonconstant sources in this family, the condition is self-duality; constants remain separate valid controls.

Every other tested source has an audited complete4-by-4 periodic witness with identical current gradient and different next gradient. This rules out every deterministic update on that observation alone, even nonlocal ones. The 3-by-3 control hides 128/254 through uniform-output aliasing. The [note](../research/2026-09-10-full-gradient-closure.md) preserves both scopes and the frozen evidence.

The observation stores two components per site and its local neighborhoods obey four plaquette constraints. Only 16,384 of 262,144 neighborhoods are realizable; no preferred off-image completion is selected. The analytic construction continues through every dimension, with d components, and intertwines replication on valid gradient fields. This gradient tower retains source dynamics modulo global complement and does not restore the absolute source state.
