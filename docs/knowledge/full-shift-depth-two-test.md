# Decide full-shift refinement depth at most two by sparse reachability in the depth-two pair graph

Completed 2026-09-12. Frozen protocol reviewed by Codex before implementation (gate 1 at corrected revision `4801d91`, after the full-shift parity deduction was written into the unit and the ring-14 and all-tested-ring gaps were separated), verifier committed before the single deterministic run, CI replay with both tiers. Held: full shift implies every ring 3 to 14 (M1); monotonicity in depth and divisibility (M2); parity depth at most two for all 256 rules on the full shift (M4); the 54 fourteenth-unit witnesses realized as eventually periodic pairs passing the trimmed-window replay (M6); complement and reflection covariance (M7). Failed: M3, that the 54 gap pairs have full-shift depth exactly two (24 have depth at least three); M5, that the all-tested-ring depth-two gap is nonempty for exactly 232, 200 and 22 (it is nonempty for all but parity). No deviation. Accepted after Codex's gate-2 sign-off at `8aa35fd` (conditional on the replay check, which passed), merged in PR #132 as `3636598`. See the [full-shift-depth-two note](../research/2026-09-11-full-shift-depth-two.md).
