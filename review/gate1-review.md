# Independent Gate 1 review

Reviewer: Codex (OpenAI), /root/relations_review, 2026-09-15.

Reviewed frozen protocol SHA-256: `7ae1ff9d736e5c6efdd5ded582f2d9ec1297fc76757cf02f01b5670d11feed0b`. This review follows recovery in the current session and precedes its implementation/evaluation; it does not backdate approval of prior work.

**Sign-off: approved** for the fixed 2,048-contract reanalysis, 78-record independent physical panel and seven prespecified affine readouts.

The longitudinal pointed action is free. Each ambiguous orbit contributes `w(w-1)/2` within-orbit equal pairs, and each pair of distinct orbits contributes `w²` event pairs. Hence `E = w²E′ + M(w-1)/2`, `O = w²O′`, and the quotient relation rank is `M/w - U`. The exact full pointed-state byte quotient removes the remaining spatial translation copies. The symbolic parity test correctly cancels shared completion variables. Missing relation denominators remain not applicable.

Two implementation clarifications were agreed before evaluation:

- The sibling-only Boolean no-go requires zero relation rank on the **child** floor, plus disjoint all-free six-variable child blocks. Zero parent rank alone is insufficient. Report the verified domain; make no claim for fixed or mixed blocks.
- A failed address-map quotient or injectivity check invalidates that transport comparison and must be reported as a failure/not applicable. Do not discard failing classes and calculate survival on the remainder.

The fixed panel, hash-pinned inputs, synthetic controls, separate width reports, off-CI ten-minute/2-GiB budgets, complete preservation and bounded nonclaims are suitable. Native evolution is checked against original source truth tables; Class IV labels do not enter fitted thresholds. This Gate 1 approval does not supply Gate 2 sign-off on results.
