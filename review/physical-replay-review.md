# Independent physical replay and result comparison

Reviewer: Codex (OpenAI), /root/relations_review, 2026-09-15.

The independent replay agrees exactly with the primary scientific output at SHA-256 `5101ad951144527fad7f32bc3819da3b540fe3320fde34478ca99fe136fa71b3`.

Scope checked:

- All 78 original archived physical records, using immutable archive/member hashes and the full D2 table hash.
- All 104 symbolic contracts, including equivalence partitions and affine constants at 1,317,888 pointed events. Different internal variable labels were compared by their exact equality partitions.
- Full spatial orbit partitions and their signed relation counts, all 2,048 longitudinal census contracts, and all 546 contract/readout transport summaries.
- All six fixed-phase address maps at every adjacent interface respect the exact full-state quotient and are injective.
- Child sibling-block fixed/free counts and the zero-rank condition agree. The conditional Boolean no-go applies to distinct all-free blocks on a verified child floor; it makes no claim about mixed blocks, fixed outputs, overlapping spatial readouts or constrained completions.

The replay gathers literal spatial coordinates and packs exact neighborhood bytes. It does not import the primary DAG keyer, quotient implementation, symbolic extractor or transfer code. Native successors are reconstructed from archived derivative constraints, using independently verified spatial equivariance to recover longitudinal positions, and checked against elementary root truth tables. Exact full-state byte keys supply the spatial quotient.

The independent scientific replay took 148.969 seconds with peak RSS 125,324 KiB. The primary run took 44.082 seconds with peak RSS 189,092 KiB. Both completed outside Actions within the frozen ten-minute and two-GiB budgets. Synthetic physical-key and counting controls preceded replay evaluation.

The replay's executed source is `review/replay_relations.py`, SHA-256 `31cb99795c11275443876ddf7a2df51c61117930ef67e6bd5ec9835e1c72ac9f`. Its preserved output is `review/replay/result.json`, SHA-256 `7b55f1cef44ac3f1b5a4f943a33a38d352e35acd154f917113939eb7ed1d9fa7`. The exact comparison is recorded in `review/comparison.json` and implemented in `review/compare_relations.py`.

`review/replay_portable.py` changes only input/output paths before invoking the preserved executed source. It requires the complete hash-matching original source archive, original manifest, full D2 table and prior completion result. Its controls-only invocation passed. Existing completed output must be preserved; select a new output directory for a replay.

Scientific interpretation: zero higher-floor ranks for width-seven rules 54/110 are exact in that finite domain. Width eight retains relations also present in controls 18/126, and color-complement representatives behave differently from 54/110 at width seven. These observations do not establish a Class IV discriminator or a symmetry-invariant property. Further spatial quotienting cannot repair an already-zero relation rank. The result does not rule out other observables, completion constraints, transports or a universal beam.

**Scientific implementation and result review: no remaining blockers.** This record is scoped to the pinned scientific bytes, and does not constitute final repository Gate 2 sign-off on an as-yet-unreviewed publication head.
